<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_homepage') }}">{{ 'app.navbar.name'|trans }}</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <div class="d-flex flex-row">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_reservation_index') }}">{{ 'app.navbar.home'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_stylist_index') }}">{{ 'app.navbar.about'|trans }}</a>
                    </li>
                </ul>
            </div>
        </div>
        <div class="d-flex flex-row">
            <a class="nav-link text-black" href="{{ path('app_login') }}">
                <i class="fas fa-lock"></i>
                {{ 'app.navbar.login'|trans }}
            </a>
        </div>
    </div>
</nav>
